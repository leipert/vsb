box: olger/box-yeoman
# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `npm install` command
    - script:
        name: Copying config
        code: cp $CONFIG_FILE app/overwrite.js
    - npm-install
    - leipert/yeoman-bower-install
    - script:
        name: Test and build gsb
        code: gulp
    - script:
        name: Copying graphs
        code: cp config/graphs/*.ttl dist/

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: dist